# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Mobile Dropdown Displays Universities and DSI Campus Data Correction
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For the mobile rendering issue, use remote debugging on real devices (iOS/Android); for DSI data, scope to concrete failing case
  - Test implementation details from Bug Condition in design:
    - Mobile device (iOS or Android) opens university dropdown with populated universities array but renders "No options available"
    - DSI campus query returns 4 campuses including DSIT instead of 3
  - The test assertions should match the Expected Behavior Properties from design:
    - Mobile dropdown should display all 5 universities with proper rendering
    - DSI should return exactly 3 campuses (DSU, DSCE, DSATM) without DSIT
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Mobile devices show "No options available" despite universities.length === 5
    - Console logs show universities loaded but dropdown renders empty state
    - DSI campus array contains 4 items instead of 3
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Desktop, Emulator, and Other University Data Preservation
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Desktop browser dropdown displays all 5 universities correctly
    - Mobile emulator (Chrome DevTools) dropdown displays all 5 universities correctly
    - PES University returns 3 campuses (Electronic City, Ring Road, Hanumanth Nagar)
    - Single-campus universities (RV, REVA, UVCE) auto-select campus
    - Form submission processes university/campus selections successfully
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - For all desktop browser interactions, dropdown renders all universities
    - For all mobile emulator interactions, dropdown renders all universities
    - For all non-DSI university queries, campus data remains unchanged
    - For all form submissions with valid selections, processing succeeds
  - Property-based testing generates many test cases for stronger guarantees across devices and browsers
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix for mobile dropdown university loading and DSI campus data

  - [x] 3.1 Remove DSIT campus from Dayananda Sagar Institutions
    - Open `server/src/data/universities.js`
    - Locate Dayananda Sagar Institutions entry in the universities array
    - Remove the `{ name: 'DSIT' }` entry from the campuses array
    - Verify remaining campuses are DSU, DSCE, DSATM in that order
    - Verify campuses array length is now 3
    - _Bug_Condition: input.dataQuery === 'getDSICampuses' AND returnedCampuses.includes('DSIT')_
    - _Expected_Behavior: DSI returns exactly 3 campuses (DSU, DSCE, DSATM) without DSIT_
    - _Preservation: PES University 3 campuses, single-campus universities, other university data unchanged_
    - _Requirements: 1.3, 2.3, 2.4, 3.3, 3.4, 3.5_

  - [x] 3.2 Fix mobile dropdown rendering issue in CustomSelect
    - Open `src/components/ui/CustomSelect.jsx`
    - Add useEffect to force re-render when options prop changes on mobile devices
    - Review and fix touch event handlers to ensure they don't prevent dropdown rendering
    - Add defensive checks for options array before mapping (verify options.length > 0)
    - Add console logging for mobile debugging (log options array, isOpen state)
    - Review dropdown positioning for mobile browsers (verify position: absolute and z-index work correctly)
    - Verify all mapped elements have stable key props (currently using option.value)
    - Test changes using remote debugging on real iOS and Android devices
    - _Bug_Condition: input.device IN ['iOS', 'Android'] AND universitiesArray.length > 0 AND renderedOptions.length === 0_
    - _Expected_Behavior: Mobile dropdown displays all 5 universities with proper styling and touch interaction_
    - _Preservation: Desktop and emulator dropdown functionality unchanged_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1, 3.2_

  - [x] 3.3 Add mobile-specific logging to Login and Signup pages
    - Open `src/pages/auth/Login.jsx` and `src/pages/auth/Signup.jsx`
    - Add console logs in useEffect to track state updates on mobile:
      - Log when universities state is set
      - Log the options array passed to CustomSelect
      - Log universities.length and options.length
    - Verify setUniversities triggers re-render on mobile devices
    - Test using remote debugging on real mobile devices
    - _Bug_Condition: Mobile device state updates not triggering re-render_
    - _Expected_Behavior: State updates trigger proper re-renders on mobile_
    - _Preservation: Desktop and emulator state management unchanged_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1, 3.2_

  - [x] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Mobile Dropdown Displays Universities and DSI Campus Data Correction
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1 on real mobile devices and verify DSI data
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify mobile dropdown displays all 5 universities on iOS and Android
    - Verify DSI returns exactly 3 campuses without DSIT
    - _Requirements: Expected Behavior Properties from design (2.1, 2.2, 2.3, 2.4)_

  - [x] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Desktop, Emulator, and Other University Data Preservation
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm desktop dropdown still works correctly
    - Confirm mobile emulator dropdown still works correctly
    - Confirm PES University still has 3 campuses
    - Confirm single-campus universities still auto-select
    - Confirm form submission still processes correctly
    - _Requirements: Preservation Requirements from design (3.1, 3.2, 3.3, 3.4, 3.5, 3.6)_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all tests (bug condition exploration + preservation property tests)
  - Verify mobile dropdown works on real iOS devices (iPhone 13+, iOS 15+)
  - Verify mobile dropdown works on real Android devices (Android 10+)
  - Verify desktop and emulator functionality unchanged
  - Verify DSI campus selection shows exactly 3 options
  - Verify form submission works with all universities and campuses
  - If any issues arise, document them and ask the user for guidance
